export interface Columns {
    header: string;
    headrekey: string;
    isImg?:boolean;
    isLink?: boolean;
    isLinkActions?: any;
    // isRoute?: boolean;
    // isRouteActions?: any;
    // isButton?: boolean;
    // buttonActions?:any,
    // userImg?: boolean;
}
